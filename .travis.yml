language: python
python:
    - 2.7
    - 3.4
    - 3.5
    - 3.5-dev
    - 3.6-dev
install:
    - pip install -e .
    - pip install coveralls nose requests-mock
script:
    - coverage run --source=xirvik '--omit=xirvik/test/*' -m nose -x
after_success:
    coveralls
